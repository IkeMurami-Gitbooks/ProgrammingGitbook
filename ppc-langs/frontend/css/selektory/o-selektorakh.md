# О селекторах

Селекторы — элементы, к которым применяются стили

Селекторами могут быть:

* теги (`h2`, `p`, `body`, ...)
* классы (`.card`, `.item`, ...)
* идентификаторы

Например:

```css
.card {
    padding: .5rem 1rem;
}

h2 {
    margin-bottom: .5rem;
}

#someid {
    color: green;
}
```
